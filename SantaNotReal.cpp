// Santa Is Not Real (C++ because why not?)
// This is intended to be a placeholder file so GitHub will mark this repo as C++, which might make it easier to find.
#include <iostream>

int main(void)
{
    std::cout << "Santa is NOT real!\n";
    return 0;
}
